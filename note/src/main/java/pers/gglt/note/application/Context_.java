package pers.gglt.note.application;

public class Context_ {

    /**分类*/
    // this (Act实例)(扩展了context)
    // Act.context ()
    // App.context ()
    // base.context (不推荐使用)(返回由构造函数指定/setBaseContext()设置的上下文)

    /**概念*/
    //

    /**作用*/
    // 获取资源和类....

    /**注意点*/
    /**内存泄漏*/
    // 避免 (确保Act中对象的生命周期在Act内)(避免由于对象对Act的引用导致其不能被销毁)
}
